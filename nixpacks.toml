[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build --filter=web"]

[start]
cmd = "cd apps/web && bun run start"

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
PORT = "3000"
